# dependencies
node_modules
.pnp
.pnp.js

# remotion
out
build
.remotion

# rendered output
public/renders

# env / secrets
.env
.env.local
.env*.local

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# os / editor
.DS_Store
.idea
*.swp

# whisper models (large, downloaded on demand)
whisper.cpp
models

# stray / non-template files
/ahmad-landing.html

# media added via the dashboard (runtime assets, not template source).
# the folder structure is kept via .gitkeep; uploaded files are not committed.
# to version a specific asset anyway: git add -f public/images/your-file.png
/public/images/*
/public/videos/*
/public/audio/*
/public/files/*
/public/casts/*
!/public/**/.gitkeep
# template sample assets referenced by the component-library demos
!/public/videos/robot_nature.mp4
!/public/images/sample-screenshot.png

# transcription artifacts produced by /transcribe
*.captions.json
*.srt
*.transcript.txt
*.16k.wav
